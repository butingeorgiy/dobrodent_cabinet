import BaseEditPopupView from "../../base/BaseEditPopupView";

export default class EditPopupView extends BaseEditPopupView {
    constructor(btn, error, success, content) {
        super(btn, error, success, content);
    }
}
