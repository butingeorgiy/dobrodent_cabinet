import BaseProfileFormView from "../../base/BaseProfileFormView";

export default class PatientProfileFormView extends BaseProfileFormView {
    constructor(formNode) {
        super(formNode);
    }
}
